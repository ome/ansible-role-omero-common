---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  # The two containers test that the "reload systemd" handler can be
  # notified regardless of systemd status
  - name: omero-common
    image: centos:7
  - name: omero-common-systemd
    image: centos/systemd:latest

provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
